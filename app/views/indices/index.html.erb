<p id="notice"><%= notice %></p>

<head>
</head>
<body> 
  <!-- Hero Image -->
  <div class="hero-image">
    <!--<img src="<%= @hero_image %>" />-->
  </div>
  <!-- Hero Quote -->
  <div>
      <h1>
      “I have always imagined that Paradise will be a kind of library.”
      ― Jorge Luis Borges
      </h1>
  </div>
  <!-- Search Bar -->
  <nav>
    <h3>Search for your favourite books</h3>
    <form action="<%= books_path %>" method="GET" class="search-bar">
        <input type="search" name="search" />
    </form>
  </nav>





</body>

<!-- <table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @indices.each do |index| %>
      <tr>
        <td><%= link_to 'Show', index %></td>
        <td><%= link_to 'Edit', edit_index_path(index) %></td>
        <td><%= link_to 'Destroy', index, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Index', new_index_path %>
-->
